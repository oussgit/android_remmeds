<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- divider horizontal classique -->
    <style name="style_Divider">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">1dp</item>
        <item name="android:background">?android:attr/listDivider</item>
    </style>

    <!-- theme des switchs -->
    <style name="style_switch" parent="Theme.AppCompat.Light">
        <!-- activé -->
        <item name="colorControlActivated">@color/bleu_remmeds</item>
        <!-- inactif -->
        <item name="colorSwitchThumbNormal">#f1f1f1</item>
        <!-- inactif interactif -->
        <item name="android:colorForeground">#42221f1f</item>
    </style>

    <!-- theme des toggle button -->
    <style name="style_togglebutton" parent="Theme.AppCompat.Light">
        <!-- activé -->
        <item name="colorControlActivated">@color/bleu_remmeds</item>
        <!-- inactif -->
        <item name="colorSwitchThumbNormal">#f1f1f1</item>
        <!-- inactif interactif -->
        <item name="android:colorForeground">#42221f1f</item>
    </style>

    <!-- theme des check boxes -->
    <style name="style_checkbox" parent="Theme.AppCompat.Light">
        <!-- activé -->
        <item name="colorControlActivated">@color/bleu_remmeds</item>
        <!-- inactif -->
        <item name="colorSwitchThumbNormal">#f1f1f1</item>
        <!-- inactif interactif -->
        <item name="android:colorForeground">#42221f1f</item>
    </style>

    <!-- theme des edit text -->
    <style name="style_edittext" parent="Theme.AppCompat.Light">
        <!-- activé -->
        <item name="colorControlActivated">@color/bleu_remmeds</item>
        <!-- inactif -->
        <item name="colorSwitchThumbNormal">#f1f1f1</item>
    </style>

</resources>
